[package]
name = "scene-layout"
description = "Resolved elements + measure → positioned boxes per frame (pure geometry)"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
scene-ir.workspace = true
scene-time.workspace = true
